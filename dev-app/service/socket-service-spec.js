describe('socketService', function() {

  beforeEach(module('socketPuppetApp'));

  it('should ...', inject(function(socketService) {

    //expect(socketService.doSomething()).toEqual('something');

  }));

});
