angular.module('socketPuppetApp').controller('SceneCtrl',function(
    $scope,
    mySocket
){

   /* mySocket.connect();

    mySocket.on('connect', function(event){

        console.log('socket has connected');

        mySocket.emit('message', 'angular-socket is set up');

        mySocket.on('message', function(data){
            console.log(data);
        });

    });*/

});
